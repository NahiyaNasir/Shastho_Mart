model Categories {
  id          String         @id @default(uuid())
  name        String         @unique
  image       String?
  description String?

  medicines   Medicine[]
  slug        String         @unique

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}